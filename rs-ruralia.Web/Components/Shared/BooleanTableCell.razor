@* Reusable table cell for displaying boolean values *@

@if (Value == true)
{
    <MudChip T="string" 
             Size="Size.Small" 
             Color="@TrueColor" 
             Icon="@TrueIcon">
        @TrueText
    </MudChip>
}
else if (Value == false)
{
    <MudChip T="string" 
             Size="Size.Small" 
             Color="@FalseColor" 
             Icon="@FalseIcon">
        @FalseText
    </MudChip>
}
else
{
    <MudText>@NullText</MudText>
}

@code {
    [Parameter]
    public bool? Value { get; set; }

    [Parameter]
    public string TrueText { get; set; } = "Yes";

    [Parameter]
    public string FalseText { get; set; } = "No";

    [Parameter]
    public string NullText { get; set; } = "—";

    [Parameter]
    public Color TrueColor { get; set; } = Color.Success;

    [Parameter]
    public Color FalseColor { get; set; } = Color.Default;

    [Parameter]
    public string TrueIcon { get; set; } = Icons.Material.Filled.Check;

    [Parameter]
    public string FalseIcon { get; set; } = Icons.Material.Filled.Close;
}
